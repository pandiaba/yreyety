on:
  pull_request:
    types: [opened, reopened]
  push:
    branches: main
env: 
  env_var: ${{vars.NOYEISON }}
  
jobs:
   aca:
    name: "Cual sera el valor de la variable"
    environment: Desa
    runs-on: ubuntu-latest
    steps:
      - name: "Environment"
        id: echoenv
        run: echo "yeison $env.YEISON"
        env:
          env_var: ${{vars.NOYEISON }}
      - name: "Environment"
        id: echoenvno
        run: echo "noyeison $env.NOYEISON  env_var $env_var"
